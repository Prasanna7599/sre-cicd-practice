name: My second workflow

on: 
  Push: 
     branches: [main]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
    - name:checkout code
      uses: actions/ checkout@v4
    - name: Say Hello
      run: echo "Hello from Github Actions, running on fresh machines!"
    - name: Show current date
      run: date
